import React from 'react';
import { Container } from 'react-bootstrap';
import Header from '../Header/Header';

const Home = () => {
    return (
        <div>
           <Container>
            <Header></Header>
           </Container>
        </div>
    );
};

export default Home;